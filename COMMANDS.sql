-- some ideas for functions:
--
-- searchByCity(city):
-- table showing ( c_last, c_first, c_city, c_state, code_desc,
--                start_date, end_date)

-- searchByZipCode(zipcode)
-- same as above

-- makePayment(amount)
-- amount_paid += amount

--